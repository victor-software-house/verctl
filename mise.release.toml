# Loaded when MISE_ENV contains `release`.
# Published tarball only. No rust.

[tools]
"github:victor-software-house/verctl" = "0.0.2"

[tasks.ver]
description = "Released verctl binary"
run = "verctl"
